{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaires list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Album</th>
                <th>Utilisateur</th>
                <th>Commentaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% set i = 0 %}
        {% for commentaire in commentaires %}
            {% set i = i + 1 %}
            <tr class="row{{ i % 2 }}">
                <td><a href="{{ path('commentaire_show', { 'id': commentaire.id }) }}">{{ commentaire.id }}</a></td>
                <td><a href="{{ path('_show', { 'id': commentaire.album.id }) }}">{{ commentaire.album }}</a></td>
                <td>{{ commentaire.utilisateur }}</td>
                <td>{{ commentaire.commentaire }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('commentaire_show', { 'id': commentaire.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentaire_new') }}">Ecrire un nouveau commentaire</a>
        </li>
        <li>
            <a href="{{ path('_index') }}">Retourner à la liste des albums</a>
        </li>
    </ul>
{% endblock %}
