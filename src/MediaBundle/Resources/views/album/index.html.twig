{% extends 'base.html.twig' %}

{% block body %}
    <h1>Albums list</h1>

    <table class="responsive-table bordered striped center">
        <thead class="center">
            <tr>
                {#<th>Id</th>#}
                <th>Titre album</th>
                <th>Artiste</th>
                <th>Genre</th>
                <th>Support</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for album in albums %}
            <tr>
                {#<td><a href="{{ path('album_show', { 'id': album.id }) }}">{{ album.id }}</a></td>#}
                <td>{{ album.titreAlbum }}</td>
                <td>{{ album.artiste }}</td>
                <td>{{ album.genre }}</td>
                <td>{{ album.support }}</td>
                <td>
                    <ul>
                        {#<li>#}
                            {#<a href="{{ path('album_show', { 'id': album.id }) }}">show</a>#}
                        {#</li>#}
                        <li>
                            <a href="{{ path('album_edit', { 'id': album.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('album_new') }}">Create a new album</a>#}
        {#</li>#}
    {#</ul>#}
{% endblock %}
